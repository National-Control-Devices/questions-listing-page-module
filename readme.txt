1. Create content type with machine name “question”.
2. Create comment type with machine name “answers”.
3. Add the above comment type field into the content type using manage fields with machine name “field_qus_answers”.